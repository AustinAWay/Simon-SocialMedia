# Anthropic key — used for text steps only (inflection, b-roll spotting, title/description).
# All image/video/audio generation runs through the Higgsfield CLI (`higgsfield auth login` handles its own auth).
ANTHROPIC_API_KEY=sk-ant-...
